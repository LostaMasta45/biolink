import { NextResponse } from "next/server"; import { createClient } from "@supabase/supabase-js"; export async function GET() { const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!); const { data: tx } = await supabase.from("transactions").select("*"); const { data: orders } = await supabase.from("payment_orders").select("*"); const { data: posts } = await supabase.from("posting_queue").select("*"); return NextResponse.json({ tx, orders, posts }); }
